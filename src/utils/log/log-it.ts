/**
 * Logs a message to the console with the provided name.
 *
 * @param name - The name or message to be logged.
 */
export function logIt(name: string) {
  console.log(`log it: ${name}`);
}
