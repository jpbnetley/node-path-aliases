/**
 * Logs a message to the console with the provided name.
 *
 * @param name - The name or string to include in the log message.
 */
export function logMe(name: string) {
  console.log(`logging me: ${name}`);
}
