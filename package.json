{
  "name": "node-starter",
  "version": "0.0.0",
  "type": "module",
  "imports": {
    "#*": "./dist/*"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
